package com.tyss.capgemini.inheitance;
//Marker Interface.
public interface MethodsInterface2 {//an interface can be empty and its called marker interface(no methods inside the interface).
//marker interface is used to give annotations
	//just to give some special behaviour to elements(class,..etc)
}
